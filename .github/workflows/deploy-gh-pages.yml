# This workflow is deprecated in favor of deploy-pages.yml.
# It has been disabled to prevent duplicate/conflicting deployment runs.

name: Deprecated Deployment Workflow

on:
  push:
    branches:
      - _disabled_
